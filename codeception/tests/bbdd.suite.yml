actor: BbddTester
modules:
    enabled:
        - Db:
            dsn: "mysql:host=%host%:%port%;dbname=%dbname%"
            user: "%dbuser%"
            password: "%dbpassword%"
        - Asserts
        - \Helper\Bbdd
        # - Doctrine2:
        #     # depends: Symfony
        #     # Call the static method `MyDb::createEntityManager()` to get the Entity Manager
        #     connection_callback: ['CodeceptionManager', 'createEntityManager']